# 1. 🎯 Цель работы
# Цель:
# Освоить принципы архитектурного паттерна MVC (Model–View–Controller) и применить их при создании консольного Python-приложения для анализа и преобразования нуклеотидных последовательностей ДНК и РНК.
# Основные задачи:
# Изучить структуру MVC и разделение ответственности.
# Разработать модель данных (ДНК/РНК).
# Реализовать функционал биоинформатического анализа (валидация, транскрипция, трансляция, поиск ORF и т.д.).
# Создать консольное меню (View).
# Разработать контроллер для управления взаимодействием между пользователем и моделью.
# 2. 📚 Теоретическая основа
# 🔬 Биологический контекст
# ДНК (DNA) состоит из нуклеотидов A, T, G, C.
# РНК (RNA) — из A, U, G, C.
# Транскрипция — процесс синтеза РНК на основе ДНК (T заменяется на U).
# Обратная транскрипция — замена U обратно на T.
# Комплементарность — A↔T(U), G↔C.
# GC% — доля гуанина и цитозина в цепи (важно для устойчивости).
# Трансляция — считывание РНК триплетами (кодонами) в аминокислоты по таблице кодонов.
# ORF (Open Reading Frame) — участок РНК от старт-кодона (AUG) до стоп-кодона (UAA, UAG, UGA).
from OOP_MVC_DNK_RNK_view import ConsoleView
from OOP_MVC_DNK_RNK_controller import Controller

def main():
    view = ConsoleView()
    controller = Controller(view)
    controller.run()

if __name__ == "__main__":
    main()